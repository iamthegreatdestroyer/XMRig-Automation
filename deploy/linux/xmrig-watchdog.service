[Unit]
Description=XMRig busy-watchdog (continuous; yields the miner to builds and ollama inference)
After=xmrig.service

[Service]
Type=simple
ExecStart=/opt/xmrig/xmrig-watchdog.sh
Restart=always
RestartSec=5
Nice=-5

[Install]
WantedBy=multi-user.target
